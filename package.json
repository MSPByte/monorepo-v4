{
  "name": "monorepo-v4",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:agent": "turbo run dev --filter=agent --filter=agents",
    "dev:web": "turbo run dev --filter=frontend --filter=@mspbyte/shared --filter=@mspbyte/trpc",
    "dev:pipeline": "INGESTION_SCHEDULER_ENABLED=false turbo run dev --filter=ingestion --filter=normalize --filter=policies",
    "dev:pipeline:scheduled": "turbo run dev --filter=ingestion --filter=normalize --filter=policies",
    "infra:up": "docker compose -f infra/docker-compose.yml up -d",
    "infra:down": "docker compose -f infra/docker-compose.yml down",
    "infra:clean": "docker compose -f infra/docker-compose.yml down -v",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "nitro": "npm:nitro-nightly@latest",
    "prettier": "^3.7.4",
    "prettier-plugin-svelte": "^4.1.1",
    "turbo": "^2.9.18",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "backend/*",
    "infra/*",
    "packages/*"
  ]
}
